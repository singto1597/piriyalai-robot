void Box1(){ //วางลูกบาศก์ที่ 1
  /*MaxForward(AccSpeed,480);
  ForwardBalance(SlowSpeed,310);

  TurnRight();
  MaxForward(AccSpeed,280);
  ForwardBalance(SlowSpeed,310);

  TurnRight();
  MaxForward(AccSpeed,120);
  ForwardBalance(SlowSpeed,310);

  TurnRight();
  MaxForward(AccSpeed,150);

  TurnLeftTime(140);
  MaxForward(AccSpeed,360);
  ForwardBalance(SlowSpeed,310);

  UTurn();
  MaxForward(AccSpeed,135);
  MotorStop();

  TurnRight();
  BackwardBalance(SlowSpeed,310);

  MaxForward(AccSpeed,320);
  ForwardBalance(SlowSpeed,380);

  TurnRight();
  MaxForward(AccSpeed,120);
  ForwardBalance(SlowSpeed,310);

  
  TurnRight();
  MaxForward(AccSpeed,90);
  ForwardBalance(SlowSpeed,0);

  KickFront();
  MotorStop();*/
  MaxForward(AccSpeed,580);
  MotorStop();

  MaxTurnRight();
  MaxForward(AccSpeed,360);
  MotorStop();

  MaxTurnRight();
  MotorStop();
  MaxForward(AccSpeed,120);
  MotorStop();
  MaxForwardBalance(25,310);
  
  MaxTurnRight();
  MaxForward(AccSpeed,165);

  TurnLeftTime(147);
  MotorStop();
  MaxForward(AccSpeed,300);
  MotorStop();
  MaxForwardBalance(25,310);

  
  KickFront();
  
  MotorStop();
  
}
  
